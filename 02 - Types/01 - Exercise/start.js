// 1. Create a person object that has name, age, and languages;
// 2. Print out "My name is <name>. I'm <age> years old. I can speak <languages>" in the console
const person = { name: 'kei', age: 10, languages: 'Japanese' };
console.log(
  `My name is ${person.name}.I'm ${person.age} year olds.I can speak ${person.languages}`
);
