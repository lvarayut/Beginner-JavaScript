// 1. Create a function called "readFile" which accepts "text" and "callback" parameters, which are string and function respectively
// 2. In the "readFile" function, use the JavaScript built-in function called "setTimeout" to print out the given "text" after one second
// 3. At the end of the function, call the callback function

/// Start coding here

readFile('I am reading a file', () => console.log('done'));
