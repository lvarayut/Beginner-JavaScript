// 1. Create a function called "sum" that accepts "nums" as a parameter, which is the array type
// 2. The function should loop through the "nums" and return the sum of the numbers

/// Start coding here

const nums = [5, 1, 9, 10];
console.log(sum(nums)); // Should print out 25
