// 1. Create a function called "createCalculator"
// 2. The function returns an object which has the following properties
//// 2.1 "add" is a function that accepts "a" and "b" as parameters, it should return the result of a + b
//// 2.2 "subtract" is a function that accepts "a" and "b" as parameters, it should return the result of a - b

/// Start coding here

const calculator = createCalculator();
console.log(calculator.add(5, 10));
console.log(calculator.subtract(7, 3));
