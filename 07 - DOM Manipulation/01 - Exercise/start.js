// 1. Create a function called "getRootChildElements" 
// 2. The function should return all children placed inside the root
// Bonus: Try using the children property

/// Start coding here

const child = getRootChildElements();
console.log(child);