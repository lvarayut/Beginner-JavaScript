// 1. Create a function called "updateGreeting" which accepts a "name" property
// 2. The function should get the h1 tag and update it to "Hello <name>" 
// 3. Moreover, the function should also change the text color to "chocolate"

/// Start coding here

updateGreeting('Varayut');